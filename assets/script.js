// Add functionality to the buttons using event listeners
const searchBtn = document.querySelector('.search-btn');
searchBtn.addEventListener('click', function() {
  // Search functionality
});

const bookshelfBtn = document.querySelector('.bookshelf-btn');
bookshelfBtn.addEventListener('click', function() {
  // Bookshelf functionality
});

const libraryRoomBtn = document.querySelector('.library-room-btn');
libraryRoomBtn.addEventListener('click', function() {
  // Library room functionality
});

const categoryBtn = document.querySelector('.category-btn');
categoryBtn.addEventListener('click', function() {
  // Category functionality
});

const profileBtn = document.querySelector('.profile-btn');
profileBtn.addEventListener('click', function() {
  // Profile functionality
});
